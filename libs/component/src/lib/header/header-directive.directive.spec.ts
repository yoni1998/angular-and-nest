import { HeaderDirectiveDirective } from './header-directive.directive';

describe('HeaderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
